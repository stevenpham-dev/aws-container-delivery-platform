apiVersion: v1
kind: Service
metadata:
  name: platform-api-service
  namespace: acdp
spec:
  type: LoadBalancer
  selector:
    app: platform-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000